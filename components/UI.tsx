import Link from "next/link";import {ArrowRight} from "lucide-react";
export function SectionHeading({eyebrow,title,copy}:{eyebrow:string;title:string;copy?:string}){return <div className="mb-12 grid gap-4 md:grid-cols-2"><div><p className="eyebrow">{eyebrow}</p><h2 className="title serif mt-3">{title}</h2></div>{copy&&<p className="muted max-w-xl self-end">{copy}</p>}</div>}
export function PageHero({eyebrow,title,intro}:{eyebrow:string;title:string;intro:string}){return <section className="paper-grid border-b border-black/10 py-20 md:py-28"><div className="container"><p className="eyebrow">{eyebrow}</p><h1 className="display serif mt-5 max-w-5xl">{title}</h1><p className="muted mt-7 max-w-2xl text-lg">{intro}</p></div></section>}
export function TextLink({href,children}:{href:string;children:React.ReactNode}){return <Link href={href} className="mt-6 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-[#9e1b1b]">{children}<ArrowRight size={15}/></Link>}
